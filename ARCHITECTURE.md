# Architecture

The canonical architecture overview now lives in `docs/public/architecture.md`.
Use this root file only as a compatibility pointer for older links.
For deeper design rationale and historical decisions, use `docs/design-docs/`.
